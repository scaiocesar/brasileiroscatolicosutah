{
	"name": "brasileiroscatolicosutah",
	"type": "module",
	"version": "1.0.0",
	"private": true,
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev",
		"build": "astro build && printf '%s\\n' '_worker.js' '_routes.json' > dist/.assetsignore",
		"preview": "astro preview",
		"astro": "astro",
		"db:migrate:local": "wrangler d1 migrations apply brasileiroscatolicosutah --local",
		"db:migrate:remote": "wrangler d1 migrations apply brasileiroscatolicosutah --remote",
		"db:seed": "node scripts/generate-seed.mjs && wrangler d1 execute brasileiroscatolicosutah --local --file=scripts/seed-admin.sql",
		"db:seed:remote": "node scripts/generate-seed.mjs && wrangler d1 execute brasileiroscatolicosutah --remote --file=scripts/seed-admin.sql",
		"deploy": "npm run build && wrangler deploy"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.6.12",
		"astro": "^5.17.1",
		"jpeg-js": "^0.4.4",
		"upng-js": "^2.1.0"
	},
	"devDependencies": {
		"@astrojs/tailwind": "^6.0.2",
		"@types/jpeg-js": "^0.3.0",
		"@types/node": "^24.3.0",
		"tailwindcss": "^3.4.17",
		"wrangler": "^4.33.1"
	}
}
